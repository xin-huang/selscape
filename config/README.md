# Configuration

Please use the template `main.yaml`, `selscan.yaml`, `scikit-allel.yaml`, `betascan.yaml`,`dadi-cli.yaml` and `dataset.yaml` to customize the workflow to your dataset. Configuration instructions can be found in the `Configuration` section of the [manual](https://xin-huang.github.io/selscape).
